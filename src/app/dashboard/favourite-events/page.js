"use client"

import React from 'react';

const FavouriteEvents =({liked}) => {
    console.log(liked)
    return (
        <div>
            hello
        </div>
    );
};

export default FavouriteEvents;